# 부서별 업무링크 정적 사이트 버그 수정 보고서

## 1. 개요 (Context & Purpose)
사용자가 교사 로그인(`Master` / `2026`) 상태에서 업무 카드를 신규 추가하거나 수정할 때 발생하는 두 가지 오류 현상을 해결하고자 합니다.
- **현상 1**: 구글 스프레드시트 4열(parentId)에 한글 부서명이 아닌 영문 부서 ID(예: `dept-admin`)가 기입되는 현상
- **현상 2**: 마인드맵 보드에서 2단계 업무카드 링크의 연결선(점선 화살표 엣지)이 여러 겹 겹쳐서 소용돌이 형태 등으로 중복 생성되는 현상

---

## 2. 원인 분석 (Rationale)

### 2.1 4열 영문 부서 ID 저장 오류
- **분석 결과**: 클라이언트 상태 및 화면 마인드맵 렌더링을 위해서는 부서 노드의 고유 ID(영문, 예: `dept-admin`, `dept-career`)가 링크 카드의 `parentId`로 사용되어야 정상적으로 엣지가 그려집니다. 그러나 구글 스프레드시트와의 동기화 및 저장 규약상 **스프레드시트 4열에는 한글 부서명**이 기입되도록 설계되었습니다.
- 실제로 데이터를 로드하는 시점(`src/app/page.tsx`)에는 이미 4열의 한글 부서명을 영문 ID로 역치환하여 클라이언트에 주입해주는 로직이 구현되어 있었습니다.
- 그러나 저장(POST)을 수행하는 `syncAllCardsToSheets` 함수(`src/lib/card-data.ts`) 내에서 `parentId`를 한글명으로 바꿔서 전송해주는 매핑 처리가 누락되어 영문 부서 ID가 그대로 구글 스프레드시트에 기입되고 있었습니다.

### 2.2 연결선 중복 렌더링 오류
- **분석 결과**: 구글 스프레드시트 또는 API 동기화 과정에서 카드 데이터가 중복하여 누적 생성되거나, 비동기 호출 시 동일한 고유 ID(`id`)를 가진 데이터가 여러 번 로드되는 현상이 존재했습니다.
- 클라이언트가 이 데이터를 받아 그대로 상태(`cards`)로 인스턴스화할 때 `unique` 처리가 되어있지 않아, 동일한 노드가 `cards` 배열 내에 여러 개 복사되었습니다.
- SVG 연결선을 그리는 루프(`MindMapBoard.tsx`)에서 중복된 개수만큼 패스(점선) 및 마커 화살표가 생성되어 한 곳에 여러 번 겹쳐 그려지면서 화살표 무리가 겹쳐서 렌더링되었습니다.

---

## 3. 조치 내용 (Methods Attempted)

### 3.1 4열 한글 부서명 매핑 치환 적용
- [card-data.ts](file:///c:/Users/ysj/Desktop/upload_all/%ED%95%99%EA%B5%90_%EB%B6%80%EC%84%9C%EB%B3%84_%EC%97%85%EB%AC%B4%EB%A7%81%ED%81%AC_%EC%A0%95%EC%A0%81%EC%82%AC%EC%9D%B4%ED%8A%B8/src/lib/card-data.ts)의 `syncAllCardsToSheets` 함수를 수정했습니다.
- 전송 전, 부서 노드들의 `id` -> `label` (한글 부서명) 관계를 맵핑하고, 전송 대상 `cards` 리스트 중 `type === 'link'`인 카드의 `parentId`를 해당 한글 부서명으로 치환하여 구글 시트 API로 전달하도록 조치했습니다.

### 3.2 로드 데이터 ID 기준 중복 제거 처리
- [page.tsx](file:///c:/Users/ysj/Desktop/upload_all/%ED%95%99%EA%B5%90_%EB%B6%80%EC%84%9C%EB%B3%84_%EC%97%85%EB%AC%B4%EB%A7%81%ED%81%AC_%EC%A0%95%EC%A0%81%EC%82%AC%EC%9D%B4%ED%8A%B8/src/app/page.tsx)에서 `loadCardsFromSheets()`를 통해 비동기로 로드된 데이터(`sheetsCards`)에 대해 `Map`을 이용하여 고유 ID 기준으로 중복을 걸러내는 방어 로직(`uniqueSheetsCards`)을 추가했습니다.
- 이후 변환 및 엣지 좌표 설정 시 `uniqueSheetsCards`를 사용하여 동일한 ID의 카드가 복수 개 렌더링되거나 연결선이 중첩되어 소용돌이치는 오류를 원천 차단했습니다.

---

## 4. 검증 결과 및 영향 (Results & Implications)
- **빌드 테스트**: 로컬 Next.js 컴파일 환경에서 `npm run build`를 실행하여 린트 및 빌드 과정이 `Compiled successfully`로 문제없이 완료됨을 검증했습니다.
- **예상 효과**:
  1. 구글 스프레드시트에 저장 시 4열에 `dept-admin` 대신 한글 명칭인 `행정실` 등의 명칭이 잘 저장됩니다.
  2. 로드 시 한글 부서명이 `dept-admin` 등으로 다시 안전하게 맵핑되어 기존 마인드맵 관계도가 완벽히 보존됩니다.
  3. 로드된 데이터의 중복 ID 제거를 통해 겹쳐 보이는 화살표(마커) 현상이 완벽히 해소됩니다.

---

## 5. 추가 기능 구현: 교무실 대시보드 상시 하이라이트 (Feature Request)
- **요구사항**: 교무부 산하의 '교무실 대시보드' 업무 카드에 사용자가 수동으로 하이라이트를 켜지 않더라도 기본적으로 상시 하이라이트(오렌지색 테두리 및 텍스트 강조) 효과가 적용되도록 요청받았습니다.
- **조치 내용**:
  - `DepartmentCard.tsx` 컴포넌트 내부에 '교무실대시보드'(공백 제거 기준) 노드 여부를 감지하는 로직을 추가했습니다.
  - 해당 노드일 경우 `showHighlight` 변수를 강제로 `true`로 설정하고, 시각적 주목도를 향상하기 위해 Tailwind CSS의 `animate-pulse` (은은한 박동 애니메이션) 효과를 얹었습니다.
  - 이로써 PC 및 모바일 화면 모두에서 '교무실 대시보드' 카드는 상시 하이라이트 및 펄스 효과가 정상적으로 작동합니다.

---

## 6. 추가 기능 구현: 긴 텍스트의 단어 단위 가변 줄바꿈 적용 (Word Break Keep-All)
- **요구사항**: '2028 대학입학전형 시행계획 검색기'와 같이 업무카드명이 긴 경우, 끝 글자 한 두자만 단독으로 다음 줄에 덜렁 개행(`검색\n기`)되어 가독성을 저해하는 문제를 가변적인 단어 단위 줄바꿈으로 개선하고자 합니다.
- **조치 내용**:
  - `DepartmentCard.tsx`의 2단계 업무카드 텍스트를 표현하는 `span` 태그에 Tailwind CSS의 `break-keep` (`word-break: keep-all;`) 클래스를 추가했습니다.
  - 이로써 CJK 한글 텍스트 렌더링 시 브라우저가 어절(공백) 단위로만 줄바꿈을 수행하여, 단어 중간에 글자가 끊기지 않고 `'2028 대학입학전형' \n '시행계획 검색기'` 와 같이 읽기 편한 구조로 개행이 자연스럽게 적용됩니다.

---

## 7. 추가 기능 구현: 업무 카드의 CJK 개행 모방 예측 너비 주입 (Width: 80px~190px 최적화)
- **요구사항**: 긴 글자의 카드가 줄바꿈되었을 때, 우측에 하얀 여백이 어색하게 남는 것을 없애고, 카드의 가로 크기가 전체적으로 실제 글자 단어 길이에 맞춰 타이트해지도록 개선하되, '광양고 홈페이지'나 '아이모두 전남'처럼 짧은 카드들은 줄바꿈 없이 한 줄로 깔끔하게 렌더링되기를 요청받았습니다.
- **조치 내용**:
  - 브라우저가 CJK 텍스트를 `keep-all`로 개행했을 때, 개행 후 텍스트 너비가 아닌 개행 전 너비에 가깝게 박스 가로폭을 크게 잡는 레이아웃 문제를 해결하기 위해 `grid.ts`의 `getLinkWidth` 함수에 개행 예측 픽셀 실측 알고리즘을 도입했습니다.
  - 가로 폭의 컴팩트함을 유지하면서 짧은 텍스트(예: 광양고 홈페이지, 아이모두 전남)가 강제로 줄바꿈되는 현상을 막기 위해, 링크 카드의 최대 가로 너비(`maxWidth`) 한도를 기존 `160px`에서 **`190px`**로 상향 조정했습니다.
  - 이로써 짧은 카드들은 줄바꿈 없이 한 줄로 온전히 표시되면서 글자 길이에 맞게 타이트해지고, 190px을 넘는 극도로 긴 카드들만 자연스럽게 두 줄 개행된 채 최장 라인 너비에 딱 맞춰 가로폭이 좁혀집니다.

---

## 8. 추가 기능 구현: 로그인 시 업무 카드 글자와 우측 편집/삭제 아이콘 간 빈 공간(여백) 제거
- **요구사항**: 교사 로그인 시 업무 카드의 텍스트 문자열과 우측 편집(연필) 및 삭제(휴지통) 아이콘 사이에 불필요하게 넓은 흰색 빈 공간(여백)이 생기는 현상을 해결하고 서로 바짝 밀착되도록 조정합니다.
- **조치 내용**:
  - `DepartmentCard.tsx` 내부 2단계 링크 카드의 텍스트를 감싸는 `span` 태그에 걸려 있던 **`flex-1`** 클래스를 제거하고 **`w-auto flex-none`**을 부여했습니다. 이로 인해 텍스트 영역이 우측 끝까지 불필요하게 늘어나는 것을 차단했습니다.
  - `grid.ts`의 `getLinkWidth` 함수에서 로그인 시 추가적으로 산입하던 아이콘 배치 공간인 `baseSpacing` 값을 현실적인 아이콘 세트 크기에 맞추어 `44px` ➔ **`32px`**로, 로그아웃 시 또한 `24px` ➔ **`18px`**로 대폭 단축하여 계산식 픽셀의 오차를 없앴습니다.
  - 더불어 글자 한 자당 기본 가중 픽셀 수치를 `11.2px` ➔ **`10.5px`**로 정밀 보정하여, 텍스트가 줄바꿈되었든 한 줄이든 관계없이 글자와 아이콘이 부모 카드 내부에서 딱 맞게 밀착하도록 조치했습니다.

---

## 9. 추가 기능 구현: 12자 초과 시 전체 글자수 기준 균형 있는 2줄 개행 및 스페이스 한 칸의 여유 공간 보장
- **요구사항**: 
  - 이전 `flex-none` 상태에서 발생했던 텍스트가 카드 밖으로 튀어나가고 수정/삭제 아이콘이 밀려나는 레이아웃 에러를 해결합니다.
  - 글자 수가 공백 포함 12자를 초과하면 두 줄로 자연스럽게 개행하되, 단어 한 개만 밑으로 덜렁 내려가지 않고 앞뒤 줄의 가로폭 밸런스를 맞춰 이분할 개행되도록 합니다.
  - 글자 끝 부분과 아이콘 영역 사이에 스페이스 한 칸(약 6px) 크기의 최소 안전 여백을 둡니다.
- **조치 내용**:
  - `DepartmentCard.tsx` 내부 텍스트 `span` 태그에 `flex-1 min-w-0 pr-1.5` 속성을 부여했습니다. `flex-1 min-w-0`를 통해 텍스트가 카드의 고정 너비 내에서 안전하게 줄바꿈되도록 롤백시켰고, `pr-1.5` 패딩을 통해 글자 끝과 아이콘 사이에 6px(스페이스 한 칸)의 시각적 여유 마진을 보장했습니다.
  - `grid.ts`의 `getLinkWidth` 알고리즘에 **균형 분할 밸런싱 로직**을 탑재했습니다. 단어 수 2개 이상의 긴 카드명에 대해, 단어 경계 분할점 중 앞줄과 뒷줄의 너비 편차가 최소화되는 분할 인덱스(Split Index)를 수학적으로 찾아내어 2줄 개행을 유도하고 최장 라인 길이에 맞추어 카드 박스의 실제 너비를 지정합니다.
  - 이에 따라 `'2028 대학입학전형 시행계획 검색기'`(21자)는 앞뒤 너비 밸런스를 고려하여 `'2028 대학입학전형'` \n `'시행계획 검색기'` 로 아주 균등하게 쪼개지며, 우측 여백이 메꿔진 채 안전하게 박스 내부에 들어차게 됩니다.

---

## 10. 추가 기능 구현: 로그인 여부별 카드 너비 제약 동적 분리 및 줄바꿈 최소 너비 보장
- **요구사항**:
  - 교사 로그인 시 아이콘(수정/삭제) 영역이 생기더라도, 카드의 너비가 그에 맞춰 자연스럽게 늘어나야 하며 기존 텍스트 가용 영역이 좁아져 어설프게 여러 줄로 꺾이는 문제가 없어야 합니다.
  - `'26연구학교 유공교원확인'` 과 같이 줄바꿈이 일어나는 카드가 세로로 4줄~6줄처럼 길쭉하게 늘어지는 현상을 방지하고 깔끔하게 2줄 개행 구조를 유지하도록 보장합니다.
- **조치 내용**:
  - `DepartmentCard.tsx` 및 `grid.ts` 내의 너비 조건(`minWidth`/`maxWidth`/`baseSpacing`)을 로그인 상태에 따라 엄격하게 물리적 동적 분리했습니다:
    - **로그아웃**: `minWidth = 90px`, `maxWidth = 150px`, `baseSpacing = 20px` (텍스트 가용 폭 최대 130px)
    - **로그인**: `minWidth = 122px`, `maxWidth = 185px`, `baseSpacing = 36px` (텍스트 가용 폭 최대 149px)
  - 이로써 로그인 후 아이콘(32px 영역)이 생성될 때 카드의 최소/최대 너비 상하한선 자체가 정확히 아이콘 크기만큼 동적으로 늘어나므로, 텍스트가 좁아져 쪼개지는 현상이 사라지고 가로 길이가 매우 자연스럽게 늘어납니다.
  - 줄바꿈이 실제로 트리거된 12자 초과의 카드에 대해 최소 안전 보장 가로폭(`minWidthForWrapped`)을 **로그아웃 시 120px / 로그인 시 152px** 이상으로 강제 설정했습니다.
  - 이에 따라 `'26연구학교 유공교원확인'`과 같은 카드가 극단적으로 좁은 가로폭에 갇히지 않고 120px/152px의 충분한 너비를 확보하여, 무분별하게 4줄 이상으로 쪼개지지 않고 깔끔하고 균형 잡힌 **2줄 개행**을 안정적으로 유지합니다.

---

## 11. 다크모드 해제, 집중 효과 강화 및 모바일 캔버스 최적화 (2026-06-24)
- **개선 요구사항**:
  1. 다크모드를 전면 차단하고 상시 라이트모드를 강제 적용합니다.
  2. "교무실 대시보드"의 상시 집중 효과가 사라졌던 문제를 복구하고 더욱 눈에 띄게 시각화합니다.
  3. 모바일 화면에서 기존의 일방적인 아코디언 목록 뷰 강제 방식 대신, 캔버스 뷰와 목록 뷰를 전환할 수 있는 선택 스위치 및 터치 탐색 최적화를 제공합니다.
- **조치 내용**:
  - **다크모드 해제**: [globals.css](file:///c:/Users/ysj/Desktop/upload_all/학교_부서별_업무링크_정적사이트/src/app/globals.css)에서 `prefers-color-scheme: dark` 미디어 쿼리를 제거하고 Tailwind v4에 `@custom-variant dark (&:where(.dark, .dark *));` 규칙 및 `color-scheme: light`를 지정해 다크모드를 무력화했습니다.
  - **집중 효과 복구/강화**: [DepartmentCard.tsx](file:///c:/Users/ysj/Desktop/upload_all/학교_부서별_업무링크_정적사이트/src/components/DepartmentCard.tsx)에서 대시보드 노드 검증을 부분 일치(includes)로 확장하여 공백 변형도 걸러내도록 하였고, `border-orange-500` 테두리, `scale-[1.05]`, `shadow-orange-200 shadow-xl` 및 오렌지 링 효과(`ring-2 ring-orange-500 ring-offset-1`)를 합쳐 펄스 집중 효과를 복구 및 강화했습니다.
  - **모바일 캔버스 뷰 최적화**: [MindMapBoard.tsx](file:///c:/Users/ysj/Desktop/upload_all/학교_부서별_업무링크_정적사이트/src/components/MindMapBoard.tsx)에 `mobileViewMode` 상태를 추가해 모바일에서도 목록 뷰와 마인드맵 캔버스 뷰를 오갈 수 있는 탭 스위치 UI를 제공했습니다. 또한, 캔버스 모드 시 모바일 기기 터치 드래그 팬(Pan) 및 줌 제약 조건을 해제하고, 첫 렌더링 시 전체가 눈에 보이도록 시작 줌을 `0.45`로 맞춰 중앙 정렬하는 최적화를 구현했습니다.
- **검증 결과**:
  - 로컬 `npm run build` 결과 에러 없이 성공적으로 빌드가 완료되었습니다.
  - 깃허브 `main` 브랜치 푸시와 Vercel 자동 배포 트리거를 완료했습니다.

---

## 12. 왼쪽 필터/설정 메뉴바 접기 및 토글 기능 추가 (2026-06-24)
- **개선 요구사항**:
  - 모바일 화면 가로폭이 좁은 상태에서 왼쪽 메뉴바가 화면의 3분의 1을 가려 마인드맵 캔버스 탐색이 어려운 문제를 해결하기 위해, 메뉴바를 필요에 따라 완전히 축소(접기) 및 펼치기 할 수 있는 토글 기능을 요청받았습니다.
- **조치 내용**:
  - **접기 제어 상태 신설**: [MindMapBoard.tsx](file:///c:/Users/ysj/Desktop/upload_all/학교_부서별_업무링크_정적사이트/src/components/MindMapBoard.tsx) 내부에 `isSidebarCollapsed` 상태를 신설하였습니다. 모바일 기기 접속 시 초기 화면 가림을 차단하기 위해 마운트 시 모바일 환경으로 감지되면 자동으로 `isSidebarCollapsed`를 `true`(접힘 상태)로 지정하도록 설계했습니다.
  - **접기/펴기 인터페이스**:
    - 메뉴바가 펼쳐진 상태일 때, 상단 필터 타이틀 영역 우측에 접기 아이콘(`ChevronLeft`) 버튼을 배치하였습니다.
    - 메뉴바가 완전히 접혔을 때는 메뉴바 박스를 감추고 콤팩트한 1:1 비율의 둥근 플로팅 `필터` 버튼만 띄워 놓아 화면 공간을 최대로 확보하도록 하였고, 이 버튼을 누르면 다시 부드럽게 펼쳐집니다.
    - 모바일 캔버스뿐만 아니라 데스크톱 환경에서도 해당 접기 버튼이 유기적으로 연동되도록 반응형을 고려해 다듬었습니다.
- **검증 결과**:
  - 로컬 `npm run build`를 재수행하여 문제 없이 빌드가 완료됨을 검증하였으며, 실시간 깃 푸시를 거쳐 Vercel에 신규 토글 레이아웃을 성공적으로 반영했습니다.

